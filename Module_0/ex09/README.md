# ft_package

A simple, educational Python package that provides a utility function to count occurrences of a specific item in a list.

## Installation

You can install this package locally using pip:

```bash
pip install ./dist/ft_package-0.0.1.tar.gz